import {IChatMessage} from '../../pages/Dashboard/components/Chat/interfaces/chatMessage';

const getHistory = (): Array<IChatMessage> => {
    return [];
}

const saveHistory = (history: Array<IChatMessage>): void => {
    
}